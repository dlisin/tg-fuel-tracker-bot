services:
  fuelbot:
    build:
      dockerfile: ./Dockerfile
    volumes:
      - ./local.env.yaml:/etc/fuelbot/config.yaml:ro
      - /tmp/fuelbot.db:/var/lib/fuelbot/fuelbot.db:rw
    restart: unless-stopped
